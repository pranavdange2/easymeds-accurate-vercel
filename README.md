# EasyMeds Accurate (Vercel-only)

- Next.js 14 on Vercel, no external backend.
- Per-site extractors with multiple strategies (JSON-LD, meta tags, regex).
- Candidate scoring by name similarity to improve accuracy.

Deploy: import to Vercel and ship.
